---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: deploysyncers.deploy.example.com
spec:
  group: deploy.example.com
  names:
    kind: DeploySyncer
    listKind: DeploySyncerList
    plural: deploysyncers
    singular: deploysyncer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeploySyncer is the Schema for the deploysyncers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeploySyncerSpec defines the desired state of DeploySyncer.
            properties:
              branch:
                description: GitHub branch or tag
                type: string
              checkInterval:
                description: Interval in seconds to check the deployments.yaml file
                type: integer
              repoURL:
                description: GitHub repository URL where the deployments.yaml file
                  is located
                type: string
            type: object
          status:
            description: DeploySyncerStatus defines the observed state of DeploySyncer.
            properties:
              lastAppliedTime:
                description: Last time the deployment.yaml was applied
                format: date-time
                type: string
              lastStatus:
                description: Last status of the deployment operation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
